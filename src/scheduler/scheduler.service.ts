// That will be service that periodically fetches api data, manage explorer service, decide when to send notifications
// it will depend on watchers service notification sender, explorer service, and api fetcher service
